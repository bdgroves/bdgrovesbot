# Copy this to user-config.py and fill in your credentials
# DO NOT commit user-config.py to GitHub

family = 'wikipedia'
mylang = 'en'
usernames['wikipedia']['en'] = 'Bdgroves'

# Password file (optional — pywikibot will prompt if not set)
# password_file = 'user-password.py'
